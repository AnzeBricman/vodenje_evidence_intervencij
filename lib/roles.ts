export const ROLES = {
  SUPER_ADMIN: "SUPER_ADMIN",
  ADMIN: "ADMIN",
  POVELJNIK: "POVELJNIK",
  CLAN: "CLAN",
  UPORABNIK: "UPORABNIK",
} as const;

export type Role = typeof ROLES[keyof typeof ROLES];

export const ROLE_LABEL: Record<Role, string> = {
  SUPER_ADMIN: "Super admin",
  ADMIN: "Administrator",
  POVELJNIK: "Poveljnik",
  CLAN: "Gasilec",
  UPORABNIK: "Uporabnik",
};

export const PERMISSIONS = {
  INTERVENTION_VIEW: [ROLES.SUPER_ADMIN, ROLES.ADMIN, ROLES.POVELJNIK, ROLES.CLAN],
  INTERVENTION_CREATE: [ROLES.SUPER_ADMIN, ROLES.ADMIN, ROLES.POVELJNIK],
  INTERVENTION_EDIT: [ROLES.SUPER_ADMIN, ROLES.ADMIN, ROLES.POVELJNIK],
  INTERVENTION_DELETE: [ROLES.SUPER_ADMIN, ROLES.ADMIN],

  VEHICLE_MANAGE: [ROLES.SUPER_ADMIN, ROLES.ADMIN, ROLES.POVELJNIK],
  EQUIPMENT_MANAGE: [ROLES.SUPER_ADMIN, ROLES.ADMIN, ROLES.POVELJNIK],
  USER_MANAGE: [ROLES.SUPER_ADMIN, ROLES.ADMIN, ROLES.POVELJNIK],
} as const;

export type Permission = keyof typeof PERMISSIONS;
